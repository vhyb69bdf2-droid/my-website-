// Optional: fill this in to make Create Room / Join Room sync across devices.
// Use a Firebase Realtime Database URL, not a Firestore URL.
//
// Example:
// window.CIPHERROOM_FIREBASE = {
//   databaseURL: "https://your-project-id-default-rtdb.firebaseio.com"
// };

window.CIPHERROOM_FIREBASE = {
  databaseURL: " https://cipherroom-5fd37-default-rtdb.firebaseio.com"
};
